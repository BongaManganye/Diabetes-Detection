# Diabetes-Detection
A Machine Learning system built with Python and Scikit-Learn (SVM) to predict diabetes likelihood using the PIMA dataset. Features data standardization, stratified splitting, and a single-instance prediction mechanism.
